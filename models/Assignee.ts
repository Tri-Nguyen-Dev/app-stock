export namespace Assignee {
  export interface Model {
    id?: string,
    email?: string,
    firstName?: string,
    lastName?: string,
    username?: string,
    password?: string,
    roleId?: string,
    status?: 0,
    displayName?: string,
    avatarUrl?: string,
    phoneNumber?: string
  }
}
